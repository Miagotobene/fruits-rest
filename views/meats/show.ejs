`<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meats</title>
</head>
<body>
    <h1><%= meat.name %></h1>
    <p>Temperature: <%= meat.color %></p>
    <% if (meat.readyToEat) { %>
        <p>Ready to Eat 👍🏽</p>
    <% } else { %>
        <p>Not Ready to Eat 👎🏽</p>
    <% } %>

    <a href="/meats">Go Back...</a>
</body>
</html>